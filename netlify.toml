[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# El webhook de Stripe necesita el body crudo (raw), Netlify lo maneja
# automáticamente para funciones normales de Node, no se requiere config extra.
